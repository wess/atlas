# Changelog

The release workflow quotes the section matching the version being released
(`scripts/notes.sh`), so what is written here is what people read on the
release page. Say what changed for the person installing it, not which files
moved.

A version with no section here still releases — the workflow falls back to
generated notes and warns.

## 0.1.0

First release.
