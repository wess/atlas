[package]
name = "atlas-bridge"
description = "Atlas Desktop: the one seam between a tokio runtime and the gpui main thread."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[lib]
name = "atlasbridge"
path = "src/lib.rs"

[dependencies]
gpui = { workspace = true }
tokio = { workspace = true }
futures = { workspace = true }
