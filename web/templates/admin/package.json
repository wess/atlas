{
  "name": "{{name}}",
  "type": "module",
  "scripts": {
    "dev": "atlas dev",
    "start": "bun run server.ts",
    "test": "bun test",
    "lint": "bunx biome lint src/",
    "migrate:up": "atlas migrate up",
    "migrate:down": "atlas migrate down",
    "migrate:new": "atlas migrate new"
  },
  "dependencies": {
    "@atlas/config": "latest",
    "@atlas/server": "latest",
    "@atlas/db": "latest",
    "@atlas/auth": "latest",
    "@atlas/admin": "latest",
    "@atlas/migrate": "latest",
    "@atlas/cli": "latest"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2",
    "@biomejs/biome": "2.5.14",
    "typescript": "^7.0.2"
  }
}
